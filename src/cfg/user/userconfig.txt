TextUI by szcarr. https://github.com/szcarr/TextUI
Version: V0.04

[MISC]
loginMessage = True; // When user first enters TextUI, user is prompted with a login message.

[STANDBY]
countdownPrintDelay = 02:30:00; // Value given is delay between each time program iterates and prints all countdowns.

[SYSTEM]
addToBoot = True; // Adds file to startup, meaning this program will start when computer boots.

